<?php
    require_once "../helpers/ctes.php";
    require_once "../helpers/AuthHelper.php";

    global $files;

    public static function check($files) {
        if (AuthHelper::logued()) {
            //
        }
    }